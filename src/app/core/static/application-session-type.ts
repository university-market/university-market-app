import { SessionTypeEnum } from "./session-type.enum";

/**
 * Tipo da sessão controlada pela aplicação (alterada para cada projeto U.M.)
 */
export const applicationSessionType: SessionTypeEnum = SessionTypeEnum.Estudante;